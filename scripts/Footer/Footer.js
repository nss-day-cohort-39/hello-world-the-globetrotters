const Footer = (footer) => {
    return `
    <div class="coderName">${Name}</div>
    <div class="coderCity">${City}</div>
    <div class="coderWork">${Work}</div>
    <
    `


}

export default Footer